# Plan — G1 B11–B14 Dominant Single-Slice Execution Path — 2026-03-29

## Prompt type
PLAN

## Roadmap item
G1 Execution Bundle (B11–B14)

## Objective
Establish a single fail-closed canonical `run_pqx_slice(...)` entrypoint that all PQX slice execution surfaces route through, with mandatory execution record emission, certification enforcement, and complete audit bundle output.

## Declared files

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-G1-B11-B14-2026-03-29.md | CREATE | Required plan-first artifact for a multi-file/module+contract change. |
| spectrum_systems/modules/runtime/pqx_slice_runner.py | CREATE | Canonical single-slice execution runner and fail-closed governance orchestration. |
| spectrum_systems/modules/pqx_backbone.py | MODIFY | Route legacy backbone entrypoint to canonical runner (or fail-closed). |
| spectrum_systems/modules/runtime/pqx_sequence_runner.py | MODIFY | Ensure default per-slice execution path routes via `run_pqx_slice`. |
| spectrum_systems/modules/runtime/pqx_bundle_orchestrator.py | MODIFY | Ensure bundle execution path routes via `run_pqx_slice` when no custom executor is provided. |
| scripts/pqx_runner.py | MODIFY | CLI routes to canonical runner only. |
| scripts/run_pqx_bundle.py | MODIFY | CLI run mode routes through canonical slice execution path via orchestrator defaults. |
| contracts/schemas/pqx_slice_execution_record.schema.json | CREATE | Contract for required slice execution artifact. |
| contracts/examples/pqx_slice_execution_record.json | CREATE | Golden-path example for the new contract. |
| contracts/standards-manifest.json | MODIFY | Register `pqx_slice_execution_record` contract/version pin. |
| tests/test_pqx_slice_runner.py | CREATE | Coverage for valid run, invalid step, missing/ambiguous roadmap, and bypass prevention behavior. |
| tests/test_pqx_sequence_runner.py | MODIFY | Coverage for sequence default path using canonical slice runner. |
| tests/test_pqx_bundle_orchestrator.py | MODIFY | Coverage for bundle default path using canonical slice runner. |

## Contracts touched
- `contracts/schemas/pqx_slice_execution_record.schema.json` (new)
- `contracts/standards-manifest.json` (new contract registration)

## Tests that must pass after execution
1. `pytest tests/test_pqx_slice_runner.py tests/test_pqx_sequence_runner.py tests/test_pqx_bundle_orchestrator.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
3. `python scripts/run_contract_enforcement.py`
4. `.codex/skills/contract-boundary-audit/run.sh`
5. `.codex/skills/verify-changed-scope/run.sh` with `PLAN_FILES` set to this plan's declared files

## Scope exclusions
- Do not introduce parallel/alternative runtime execution entrypoints beyond routing wrappers.
- Do not refactor unrelated runtime/governance modules.
- Do not alter roadmap content beyond execution-path wiring.

## Dependencies
- B10 queue-orchestrator and fix-gate seams remain authoritative prerequisites.
- Existing roadmap authority bridge (`docs/roadmaps/roadmap_authority.md`) must remain enforced.
