# Plan — SF-07 Coverage + Slice Dashboard (Repo-native artifacts) — 2026-03-24

## Prompt type
PLAN

## Roadmap item
SF-07 — Coverage + Slice Dashboard for spectrum-systems

## Objective
Add a deterministic, governed coverage-and-slice visibility layer that emits contract-validated JSON artifacts and a markdown summary report from eval cases/results without redesigning existing eval execution.

## Declared files
List every file that will be created, modified, or deleted.
No other files may be changed during execution of this plan.

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-SF-07-2026-03-24.md | CREATE | Record required PLAN before multi-file schema/script/test/docs work. |
| PLANS.md | MODIFY | Register this plan in the active plan table. |
| contracts/schemas/eval_case.schema.json | MODIFY | Add minimal slice metadata fields needed for coverage assignment while keeping backward compatibility. |
| contracts/examples/eval_case.json | MODIFY | Provide golden-path example carrying slice metadata. |
| contracts/schemas/eval_coverage_summary.schema.json | CREATE | Define governed machine-readable aggregate coverage artifact. |
| contracts/schemas/eval_slice_summary.schema.json | CREATE | Define governed per-slice aggregate artifact contract. |
| contracts/examples/eval_coverage_summary.json | CREATE | Golden-path example for eval_coverage_summary. |
| contracts/examples/eval_slice_summary.json | CREATE | Golden-path example for eval_slice_summary. |
| contracts/standards-manifest.json | MODIFY | Register new contracts and version updates for touched contracts. |
| data/policy/eval_coverage_policy.json | CREATE | Governed required-slice policy config consumed by coverage reporting script. |
| scripts/run_eval_coverage_report.py | CREATE | Canonical CLI entrypoint to compute coverage/slice summaries + markdown report. |
| tests/test_eval_coverage_report.py | CREATE | Deterministic focused coverage + schema + markdown report tests. |
| tests/test_contracts.py | MODIFY | Extend contract example validation coverage for new artifact contracts. |
| docs/reliability/eval-coverage-and-slices.md | CREATE | Operator documentation for SF-07 purpose, model, policy, and usage. |

## Contracts touched
- eval_case (additive metadata extension)
- eval_coverage_summary (new)
- eval_slice_summary (new)
- standards-manifest version update and contract registry entries

## Tests that must pass after execution
1. `pytest tests/test_eval_coverage_report.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
3. `python scripts/run_contract_enforcement.py`
4. `python scripts/run_eval_coverage_report.py --eval-cases contracts/examples/eval_case.json --eval-results contracts/examples/eval_result.json --dataset contracts/examples/eval_dataset.json --policy data/policy/eval_coverage_policy.json --output-dir outputs/eval_coverage`
5. `.codex/skills/verify-changed-scope/run.sh`

## Scope exclusions
- Do not add UI/frontend/dashboard web apps.
- Do not redesign eval engine execution flow or CI gate semantics.
- Do not add new third-party dependencies.
- Do not introduce new repo/module structure unrelated to coverage visibility artifacts.

## Dependencies
- Existing eval execution artifacts/contracts from SF-05 and related eval foundation slices must remain the source of truth.
