# Plan — D3L-UI-FIX-01 — 2026-04-28

## Prompt type
BUILD

## Roadmap item
D3L-UI-FIX-01

## Objective
Repair dashboard dark-mode readability, surface compliance_observation for fail-closed stale-ranking behavior, and restore bounded Overview content without backend architecture changes.

## Declared files

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-D3L-UI-FIX-01-2026-04-28.md | CREATE | Required written plan for multi-file execution. |
| apps/dashboard-3ls/app/page.tsx | MODIFY | Apply UI behavior and rendering constraints for dark mode, stale Top 3 fail-closed behavior, and Overview simplification. |
| apps/dashboard-3ls/__tests__/components/DashboardPage.test.tsx | MODIFY | Add/adjust coverage for stale ranking suppression, Overview scope limits, and Trust Pulse wording constraints. |

## Contracts touched
None.

## Tests that must pass after execution
1. `npm --prefix apps/dashboard-3ls test -- --runInBand`
2. `npm --prefix apps/dashboard-3ls run build`
3. `python scripts/run_authority_shape_preflight.py --base-ref main --head-ref HEAD --suggest-only --output outputs/authority_shape_preflight/authority_shape_preflight_result.json`
4. `python scripts/run_authority_leak_guard.py --base-ref main --head-ref HEAD --output outputs/authority_leak_guard/authority_leak_guard_result.json`
5. `python scripts/run_system_registry_guard.py --base-ref main --head-ref HEAD --output outputs/system_registry_guard/system_registry_guard_result.json`

## Scope exclusions
- Do not modify ranking algorithms or backend API ranking generation.
- Do not add new systems, graph nodes, or authority-shaped language.
- Do not weaken fail-closed behavior.

## Dependencies
None declared.

## Merge-resolution continuity notes
- Intelligence/MET sections remain out of Overview and are anchored in Diagnostics for compatibility with downstream contract-selection checks.
- Freshness-gate fail-closed behavior for Top 3 and Prioritization remains mandatory.
- Dark-mode readability classes remain required across panel/tab containers.
