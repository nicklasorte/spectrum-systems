# PLAN-NT-ALL-01-2026-04-27

## Prompt Type
BUILD

## Scope
Implement NT-ALL-01 trust compression hardening without adding new top-level 3-letter systems.

## Execution Plan
1. Add trust freshness, proof size, reason lifecycle, and certification delta policies under `contracts/governance/`.
2. Add a governance utility module to evaluate trust artifact freshness, proof size budgets, reason-code lifecycle coverage, and certification deltas.
3. Integrate policy checks into existing seams (`certification_evidence_index`, `loop_proof_bundle`, artifact tier transitive validation).
4. Add read-only operator triage CLI `scripts/print_loop_proof.py` with stable output and exit codes.
5. Add NT test suite files (`tests/test_nt_*.py`) covering red-team scenarios and fix pass behavior.
6. Add compact trust regression fixtures and tests.
7. Create delivery report `docs/reviews/NT_ALL_01_delivery_report.md` including commands/results and hard-gate confirmations.
8. Run required validations and full pytest, then commit and open PR.

## Constraints Applied
- Preserve fail-closed behavior.
- Preserve authority-shape preflight and registry validation requirements.
- No broad exclusions or silent fallbacks.
- Keep GOV/PRA as packaging seams (not policy authorities).
