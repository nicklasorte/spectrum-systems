# Plan — PYX-04 — 2026-04-14

## Prompt type
BUILD

## Scope
Diagnose and repair the real failing pytest cluster on top of PYX-03 while preserving the explicit PR-visible governed pytest check surface and all fail-closed trust semantics.

## Files in scope
| File | Action | Reason |
| --- | --- | --- |
| docs/reviews/PYX-04_failing_test_repair_review.md | CREATE | Record first-failure evidence, root-cause cluster, and exact repair strategy before code changes. |
| docs/review-actions/PLAN-PYX-04-2026-04-14.md | CREATE | Plan-first artifact for multi-file change. |
| tests/test_pqx_slice_runner.py | MODIFY | Update local preflight fixture payload to current contract schema requirements (`1.5.0` + `pytest_artifact_linkage` + updated selection integrity shape). |

## Guardrails
- Keep `.github/workflows/pr-pytest.yml` intact as the visible governed PR check.
- Do not bypass or weaken preflight/selection-integrity trust enforcement.
- Fix only the root-cause fixture/schema drift driving the failure cluster.
