# Plan — BAG — 2026-03-22

## Prompt type
PLAN

## Roadmap item
Prompt BAG — Replay Engine (Deterministic Control Replay)

## Objective
Introduce a deterministic replay engine that re-executes the canonical trust-loop path for governed inputs and emits a schema-valid replay_result artifact that classifies match, mismatch, or indeterminate outcomes.

## Declared files

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-BAG-2026-03-22.md | CREATE | Record BAG execution scope and validation requirements before build work. |
| PLANS.md | MODIFY | Register the BAG 2026-03-22 plan in the active plans table. |
| contracts/schemas/replay_result.schema.json | MODIFY | Publish BAG replay_result contract for deterministic control-path replay outcomes. |
| contracts/examples/replay_result.json | CREATE | Provide a validating golden-path replay_result example artifact. |
| contracts/standards-manifest.json | MODIFY | Register replay_result schema/example in canonical standards manifest. |
| spectrum_systems/modules/runtime/replay_engine.py | MODIFY | Add deterministic canonical-path `run_replay(...)` replay engine surface with fail-closed validation. |
| tests/test_replay_engine.py | MODIFY | Add BAG replay engine unit coverage for match/mismatch/indeterminate/canonical-path guarantees. |

## Contracts touched
- replay_result (schema update and example addition)

## Tests that must pass after execution
1. `pytest tests/test_replay_engine.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
3. `python scripts/run_contract_enforcement.py`
4. `python scripts/check_artifact_boundary.py`
5. `pytest`

## Scope exclusions
- Do not auto-trigger replay from runtime execution paths.
- Do not add async/background replay processing.
- Do not add caching, fuzzy matching, or alternate decision logic.
- Do not modify unrelated runtime modules outside replay-engine integration surface.

## Dependencies
- Prompt BAE (control-loop integration) and Prompt BAF (single-path enforcement) must remain intact because BAG reuses those canonical production paths.
