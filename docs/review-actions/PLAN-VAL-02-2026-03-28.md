# Plan — VAL-02 Fail-Closed Exhaustive Test — 2026-03-28

## Prompt type
PLAN

## Roadmap item
VAL-02 — Fail-Closed Exhaustive Test (docs/roadmap/trust_hardening_roadmap.md)

## Objective
Implement a deterministic, replayable governed fail-closed seam matrix validator that exercises runtime/governance seams with real entrypoints and emits a schema-governed summary artifact plus a thin CLI and focused integration tests.

## Declared files

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-VAL-02-2026-03-28.md | CREATE | Required PLAN artifact before multi-file BUILD/VALIDATE work. |
| contracts/schemas/fail_closed_exhaustive_result.schema.json | CREATE | New governed artifact contract for VAL-02 result summary. |
| contracts/standards-manifest.json | MODIFY | Register new contract and bump manifest publication version per contract authority rules. |
| spectrum_systems/modules/governance/fail_closed_exhaustive_test.py | CREATE | New governed module implementing seam-failure matrix and summary generation. |
| scripts/run_fail_closed_exhaustive_test.py | CREATE | Thin CLI runner for deterministic execution, artifact write, and non-zero exit on failure. |
| tests/test_fail_closed_exhaustive_test.py | CREATE | Deterministic tests covering required seams plus silent-success/ambiguous detection behavior. |

## Contracts touched
- Add `fail_closed_exhaustive_result` schema (`contracts/schemas/fail_closed_exhaustive_result.schema.json`).
- Update `contracts/standards-manifest.json` with new contract registration and version bump.

## Tests that must pass after execution
1. `pytest tests/test_fail_closed_exhaustive_test.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
3. `python scripts/run_contract_enforcement.py`
4. `.codex/skills/verify-changed-scope/run.sh`

## Scope exclusions
- Do not modify runtime decision semantics in replay/control/enforcement/xrun/backtesting modules.
- Do not introduce any parallel runtime path or fallback behavior.
- Do not refactor unrelated tests or scripts.

## Dependencies
- Existing seam modules and schemas already present in repository (replay, control, enforcement, done certification, XRUN, policy backtesting, governed failure injection).
