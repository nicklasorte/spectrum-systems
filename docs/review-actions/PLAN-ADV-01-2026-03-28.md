# Plan — ADV-01 — 2026-03-28

## Prompt type
PLAN

## Roadmap item
ADV-01 — Policy Backtesting / Scenario Simulation

## Objective
Implement a deterministic, fail-closed policy backtesting subsystem that replays governed replay artifacts through baseline and candidate policy thresholds, emits a schema-valid policy_backtest_result artifact, and provides CLI + tests.

## Declared files

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-ADV-01-2026-03-28.md | CREATE | Required PLAN artifact before multi-file BUILD. |
| contracts/schemas/policy_backtest_result.schema.json | CREATE | New governed contract for backtesting output. |
| contracts/standards-manifest.json | MODIFY | Register new contract and bump manifest version metadata. |
| spectrum_systems/modules/runtime/policy_backtesting.py | CREATE | Deterministic policy backtesting implementation. |
| scripts/run_policy_backtest.py | CREATE | CLI for running governed policy backtests from artifact paths. |
| tests/test_policy_backtesting.py | CREATE | Deterministic validation of accept/reject/review + fail-closed cases. |

## Contracts touched
- Add `policy_backtest_result` schema.
- Update `contracts/standards-manifest.json` to publish the new contract.

## Tests that must pass after execution
1. `pytest tests/test_policy_backtesting.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`

## Scope exclusions
- Do not modify replay engine execution behavior.
- Do not duplicate or alter existing evaluation control policy logic; only call it with alternate thresholds.
- Do not add probabilistic simulation behavior.
- Do not enforce promotion gating yet (hook-only semantics).

## Dependencies
- Existing replay/eval-control/error-budget/xrun artifacts and schemas remain authoritative inputs.
