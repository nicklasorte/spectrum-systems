# Plan — BAE Control Loop Integration — 2026-03-22

## Prompt type
PLAN

## Roadmap item
Prompt BAE — Control Loop Integration

## Objective
Unify control decisions behind a single deterministic runtime control-loop engine that normalizes governed signals, evaluates them, emits schema-valid `evaluation_control_decision`, and appends deterministic control trace output consumed by BN.7 control integration.

## Declared files

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-BAE-2026-03-22.md | CREATE | Required plan-first artifact for this multi-file BUILD. |
| PLANS.md | MODIFY | Register BAE plan in active plans index. |
| contracts/schemas/evaluation_control_decision.schema.json | MODIFY | Extend control decision schema to include BAE deterministic decision metadata fields. |
| contracts/examples/evaluation_control_decision.json | MODIFY | Keep canonical example aligned with updated decision schema. |
| contracts/standards-manifest.json | MODIFY | Bump `evaluation_control_decision` schema version metadata for additive contract update. |
| spectrum_systems/modules/runtime/evaluation_control.py | MODIFY | Preserve deterministic eval-summary control mapping while emitting required BAE decision metadata fields. |
| spectrum_systems/modules/runtime/control_loop.py | CREATE | Implement unified run_control_loop engine with normalization, validation, evaluation, decision, and trace stages. |
| spectrum_systems/modules/runtime/control_integration.py | MODIFY | Route decision creation exclusively through `run_control_loop` and attach `control_trace` to integration outputs. |
| tests/test_control_loop.py | CREATE | Deterministic test coverage for control-loop allow/deny/error paths. |
| tests/test_control_integration.py | MODIFY | Assert control loop is the only eval/failure decision path and preserve BN.7 output shape. |

## Contracts touched
- Modify `contracts/schemas/evaluation_control_decision.schema.json` (version bump from `1.0.0` to `1.1.0`).
- Update `contracts/standards-manifest.json` for the new `evaluation_control_decision` schema version.

## Tests that must pass after execution
1. `pytest tests/test_control_loop.py tests/test_control_integration.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
3. `python scripts/run_contract_enforcement.py`
4. `python scripts/check_artifact_boundary.py`
5. `pytest`

## Scope exclusions
- Do not add autonomous policy or heuristic decision logic.
- Do not refactor unrelated runtime chain/enforcement modules.
- Do not add replay wiring or enforcement execution from BAF/BAG.

## Dependencies
- Existing BBA `evaluation_control_decision` contract and mapper are the baseline and must remain schema-governed.
- BN.7 `control_integration` remains the authoritative runtime precondition gate.
