# Plan — CON-048 — 2026-04-02

## Prompt type
PLAN

## Roadmap item
CON-048 — Thin PQX Run CLI (Roadmap → Wrapper → Loop → Trace)

## Objective
Provide a deterministic, fail-closed CLI that ingests an ordered roadmap payload, builds canonical CON-038 wrappers, executes the existing CON-046 sequential loop, and emits/validates the authoritative CON-047 trace artifact with deterministic exit behavior.

## Declared files
List every file that will be created, modified, or deleted.
No other files may be changed during execution of this plan.

| File | Change type | Reason |
| --- | --- | --- |
| scripts/run_pqx_sequence.py | MODIFY | Extend the existing CLI seam to perform roadmap→wrapper→loop wiring and deterministic output/exit behavior. |
| tests/test_run_pqx_sequence_cli.py | CREATE | Add focused CLI coverage for ALLOW/BLOCK/REQUIRE_REVIEW, malformed roadmap fail-closed behavior, missing governed context fail-closed behavior, and deterministic output semantics. |

## Contracts touched
None.

## Tests that must pass after execution
1. `pytest -q tests/test_run_pqx_sequence_cli.py`
2. `pytest -q tests/test_pqx_sequence_runner.py`
3. `pytest -q tests/test_pqx_sequential_loop.py`
4. `pytest -q tests/test_codex_to_pqx_wrapper.py`
5. `pytest -q tests/test_contracts.py`
6. `pytest -q tests/test_contract_enforcement.py`
7. `python scripts/run_contract_enforcement.py`
8. `python scripts/run_contract_preflight.py --changed-path scripts/run_pqx_sequence.py --changed-path tests/test_run_pqx_sequence_cli.py`

## Scope exclusions
- Do not add any new decision/eval/scheduling logic.
- Do not modify wrapper semantics from CON-038.
- Do not change `spectrum_systems/modules/runtime/pqx_sequential_loop.py` behavior.
- Do not introduce additional CLI pathways beyond the thin transport seam.

## Dependencies
- CON-038 must remain the canonical wrapper builder.
- CON-046 must remain the canonical sequential execution loop.
- CON-047 must remain the authoritative sequential trace artifact.
