# PLAN-SLH-001-2026-04-17

## Prompt Type
`BUILD`

## Intent
Implement a fail-closed Shift-Left Hardening Superlayer that executes before broad validation, converts failure patterns into durable guards, enforces owner boundaries, adds targeted rerun routing, and blocks false-green certification paths.

## Seam inspection summary
- Inspected canonical ownership map and boundaries in `docs/architecture/system_registry.md`.
- Inspected contract authority surfaces in `contracts/schemas/`, `contracts/examples/`, and `contracts/standards-manifest.json`.
- Inspected runtime seams for guard/eval/replay/lineage/observability in:
  - `spectrum_systems/modules/runtime/pmh_003_surfaces.py`
  - `spectrum_systems/modules/runtime/replay_engine.py`
  - `spectrum_systems/modules/runtime/lineage_issuance_registry.py`
  - `spectrum_systems/modules/governance/system_registry_guard.py`
- Inspected enforcement/test seams in:
  - `scripts/run_system_registry_guard.py`
  - `scripts/run_contract_enforcement.py`
  - `tests/test_contracts.py`
  - `tests/test_contract_enforcement.py`

## Canonical owners (explicit)
- Authoritative gate inputs: CON, EVL, CTX, OBS, REP, LIN.
- Authoritative decision: CDE.
- Non-authoritative planning/intelligence: FRE, AIL, TST, RIL.
- Runtime pressure signaling: QOS, CAP.
- Composition-only constraint retained for TLC.

## Exact repo surfaces to modify
1. `spectrum_systems/modules/runtime/shift_left_hardening_superlayer.py` (new deterministic implementation).
2. `scripts/run_shift_left_hardening_superlayer.py` (new thin CLI prebuild + mini-cert runner).
3. `tests/test_shift_left_hardening_superlayer.py` (new deterministic tests for SL-CORE..SL-CERT and RT/FX loops).
4. `contracts/schemas/*.schema.json` (new Shift-Left result/fix/red-team/proof schemas).
5. `contracts/examples/*.json` (new examples for each new schema).
6. `contracts/standards-manifest.json` (register new contracts + version bump).
7. `docs/reviews/SLH-001_delivery_report.md` (mandatory delivery report).

## Shift-Left phases to implement
1. **SL-CORE**: unified fail-fast prebuild chain and all SL-01..SL-11 checks.
2. **SL-STRUCTURE**: concentration, multi-owner, artifact explosion, proof substitution, complexity metrics.
3. **SL-MEMORY**: exploit family registry, signature extraction, auto-eval/regression conversion, persistence + coverage gate.
4. **SL-ROUTER**: fix classification, targeted rerun planning, retry storm, capacity posture, escalation engine.
5. **SL-CERT**: mini-cert decision with eval/replay/lineage/observability/dependency/hidden-state checks.

## Red-team/fix rounds
- RT-SL-A + FX-SL-A
- RT-SL-B + FX-SL-B
- RT-SL-C + FX-SL-C
- RT-SL-D + FX-SL-D
- RT-SL-E + FX-SL-E

Each round will emit deterministic artifacts and then execute immediate fix conversion with rerun validation artifacts.

## Validation commands
- `pytest -q tests/test_shift_left_hardening_superlayer.py`
- `pytest -q tests/test_contracts.py`
- `pytest -q tests/test_contract_enforcement.py`
- `python scripts/run_contract_enforcement.py`
- `python scripts/run_system_registry_guard.py --changed-files ...`
- `python scripts/run_shift_left_hardening_superlayer.py --output ...`
- `pytest -q`

## Anti-centralization guardrails
- Keep all checks owner-specific and signal-only outside owner authority.
- Enforce multi-owner function blocker and concentration detector in prebuild.
- Keep TLC composition-only (no decision-authority migration).
- Fail on overlap/shadow ownership in registry guard surfaces.

## Mini-certification gate behavior
- CDE authoritative gate consumes outputs from SL-CORE + SL-ROUTER + SL-CERT subchecks.
- Certification fails closed if any required dimension is weak.
- Broad pytest remains downstream evidence, never substitute for mini-cert pass.
