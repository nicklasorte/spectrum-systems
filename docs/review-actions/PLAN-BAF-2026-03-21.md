# Plan — BAF ENFORCEMENT WIRING — 2026-03-21

## Prompt type
PLAN

## Roadmap item
Prompt BAF — Enforcement Wiring (MVP Phase 3)

## Objective
Wire `evaluation_budget_decision` into deterministic runtime enforcement so execution is governed by allow/warn/freeze/block outcomes with fail-closed behavior.

## Declared files
List every file that will be created, modified, or deleted.
No other files may be changed during execution of this plan.

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-BAF-2026-03-21.md | CREATE | Required plan-first artifact for this multi-file BUILD change. |
| PLANS.md | MODIFY | Register the active BAF plan in the plan index table. |
| contracts/schemas/enforcement_result.schema.json | CREATE | Define canonical schema for enforcement result artifacts. |
| contracts/examples/enforcement_result.json | CREATE | Provide golden-path example artifact for enforcement result. |
| contracts/standards-manifest.json | MODIFY | Publish the new contract and bump standards metadata. |
| spectrum_systems/modules/runtime/enforcement_engine.py | CREATE | Implement pure fail-closed enforcement mapping from budget decision to enforced outcome. |
| spectrum_systems/modules/runtime/control_executor.py | MODIFY | Add `execute_with_enforcement(bundle_path)` orchestration entry point. |
| scripts/run_enforced_execution.py | CREATE | Add thin CLI for validation -> control loop -> enforcement execution. |
| tests/test_enforcement_engine.py | CREATE | Add deterministic unit + end-to-end coverage for enforcement behavior. |

## Contracts touched
- `contracts/schemas/enforcement_result.schema.json`
- `contracts/standards-manifest.json`

## Tests that must pass after execution
1. `pytest tests/test_run_bundle_validation.py -q`
2. `pytest tests/test_run_bundle_validator.py -q`
3. `pytest tests/test_evaluation_control_loop.py -q`
4. `pytest tests/test_enforcement_engine.py -q`
5. `pytest -q`
6. `pytest tests/test_contracts.py tests/test_contract_enforcement.py -q`
7. `python scripts/run_contract_enforcement.py`
8. `.codex/skills/contract-boundary-audit/run.sh`
9. `.codex/skills/verify-changed-scope/run.sh`

## Scope exclusions
- Do not refactor legacy SLO/control-chain enforcement modules.
- Do not change run-bundle validator policy logic outside required orchestration wiring.
- Do not introduce alternate enforcement pathways outside the canonical runtime modules.

## Dependencies
- Prompt CONTROL LOOP INTEGRATION — MVP Phase 2 artifacts remain the upstream source for evaluation budget decisions.
