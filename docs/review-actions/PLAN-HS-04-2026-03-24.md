# Plan — HS-04 Routing Policy Engine — 2026-03-24

## Prompt type
PLAN

## Roadmap item
HS-04 — Routing Policy Engine

## Objective
Introduce a governed, deterministic, fail-closed routing policy decision layer that resolves prompt alias/version and model id for AG runtime execution.

## Declared files
List every file that will be created, modified, or deleted.
No other files may be changed during execution of this plan.

| File | Change type | Reason |
| --- | --- | --- |
| contracts/schemas/routing_policy.schema.json | CREATE | Add canonical routing policy contract. |
| contracts/examples/routing_policy.json | CREATE | Add golden-path routing policy artifact example. |
| contracts/schemas/routing_decision.schema.json | CREATE | Add canonical routing decision artifact contract. |
| contracts/examples/routing_decision.json | CREATE | Add golden-path routing decision artifact example. |
| contracts/standards-manifest.json | MODIFY | Register new contracts and bump manifest metadata. |
| contracts/schemas/agent_execution_trace.schema.json | MODIFY | Add routing decision linkage field(s) for runtime traceability. |
| contracts/examples/agent_execution_trace.json | MODIFY | Keep example compliant with updated trace schema. |
| spectrum_systems/modules/runtime/routing_policy.py | CREATE | Implement deterministic routing policy loader/validator/resolver module. |
| spectrum_systems/modules/runtime/agent_golden_path.py | MODIFY | Integrate routing decision path into AG runtime. |
| tests/test_contracts.py | MODIFY | Add routing policy/routing decision contract example validation coverage. |
| tests/test_routing_policy.py | CREATE | Add focused routing engine deterministic/fail-closed tests. |
| tests/test_agent_golden_path.py | MODIFY | Add routing integration and trace linkage assertions. |
| docs/runtime/routing_policy.md | CREATE | Document routing policy/decision contracts and runtime semantics. |

## Contracts touched
- `routing_policy` (new)
- `routing_decision` (new)
- `agent_execution_trace` (additive update)
- `standards_manifest` (manifest metadata + contract registry update)

## Tests that must pass after execution
1. `pytest tests/test_routing_policy.py tests/test_agent_golden_path.py tests/test_contracts.py`
2. `pytest tests/test_contract_enforcement.py`
3. `python scripts/run_contract_enforcement.py`
4. `.codex/skills/verify-changed-scope/run.sh`

## Scope exclusions
Explicitly list things that are NOT in scope for this plan.

- Do not implement fallback orchestration.
- Do not implement dynamic optimization or cost/latency tuning.
- Do not implement release/canary routing logic.
- Do not add UI/editor policy tooling.
- Do not refactor unrelated runtime modules.

## Dependencies
- HS-01 prompt registry contract/runtime path must remain the prompt resolution source of truth.
- HS-03 canonical model adapter must remain the model invocation boundary.
