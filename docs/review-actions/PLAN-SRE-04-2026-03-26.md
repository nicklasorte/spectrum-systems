# Plan — SRE-04 Regression Suite Hardening — 2026-03-26

## Prompt type
PLAN

## Roadmap item
SRE-04 | Regression Suite | Baseline + regression lock

## Objective
Harden the runtime regression harness so it deterministically compares baseline replay artifacts vs current replay artifacts, emits governed regression results, and fail-closes on missing/malformed/incompatible prerequisites.

## Declared files
| File | Change type | Reason |
| --- | --- | --- |
| spectrum_systems/modules/runtime/regression_harness.py | MODIFY | Add baseline/current replay artifact loading, deterministic comparison, fail-closed prerequisite validation, and governed mismatch artifactization. |
| contracts/schemas/regression_suite_manifest.schema.json | MODIFY | Require baseline/current replay artifact references in suite entries. |
| contracts/schemas/regression_run_result.schema.json | MODIFY | Extend governed regression_result fields with baseline refs, compared refs, and mismatch summary. |
| tests/test_replay_regression_harness.py | MODIFY | Add/adjust tests for pass, fail-closed cases, mismatch artifactization, incompatible type blocking, and deterministic behavior. |

## Contracts touched
- `contracts/schemas/regression_suite_manifest.schema.json`
- `contracts/schemas/regression_run_result.schema.json`

## Tests that must pass after execution
1. `pytest tests/test_replay_regression_harness.py`
2. `pytest tests/test_contracts.py`

## Scope exclusions
- Do not redesign replay engine execution semantics.
- Do not modify policy or enforcement modules.
- Do not create a parallel regression framework.
- Do not mutate production replay artifacts.

## Dependencies
- SRE-03 replay engine must remain authoritative for replay_result contract semantics.
