# Plan — HS-X3 — 2026-03-24

## Prompt type
PLAN

## Roadmap item
HS-X3 — Prompt Injection Defense Layer

## Objective
Add a narrow, deterministic prompt-injection detection and enforcement layer at the context-bundle to agent-execution seam so instruction-shaped untrusted content is preserved as data but cannot mutate governed runtime control surfaces.

## Declared files
List every file that will be created, modified, or deleted.
No other files may be changed during execution of this plan.

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-HS-X3-2026-03-24.md | CREATE | Required PLAN artifact before multi-file BUILD scope. |
| PLANS.md | MODIFY | Register active HS-X3 plan in active plans table. |
| contracts/schemas/prompt_injection_assessment.schema.json | CREATE | Canonical governed contract for deterministic detection/enforcement output. |
| contracts/examples/prompt_injection_assessment.json | CREATE | Golden-path example for prompt injection assessment contract validation. |
| contracts/schemas/agent_execution_trace.schema.json | MODIFY | Add trace linkage fields for prompt injection assessment and enforcement outcome visibility. |
| contracts/examples/agent_execution_trace.json | MODIFY | Keep example aligned with prompt-injection trace linkage schema changes. |
| contracts/standards-manifest.json | MODIFY | Register prompt injection assessment contract and version bump touched artifact entries. |
| spectrum_systems/modules/runtime/prompt_injection_defense.py | CREATE | Implement explicit deterministic detection classes and enforcement policy/action evaluation. |
| spectrum_systems/modules/agents/agent_executor.py | MODIFY | Integrate prompt injection assessment at post-context/pre-execution seam and enforce fail-closed behavior. |
| docs/runtime/prompt_injection_defense.md | CREATE | Document detection classes, enforcement actions, policy behavior, fail-closed cases, and out-of-scope boundaries. |
| tests/test_prompt_injection_defense.py | CREATE | Focused module-level deterministic detection and policy enforcement tests. |
| tests/test_agent_executor.py | MODIFY | Add seam integration, trace linkage, control-surface non-mutation, and fail-closed behavior coverage. |
| tests/test_agent_golden_path.py | MODIFY | Verify runtime assessment trace linkage in canonical AG path with deterministic behavior. |
| tests/test_contracts.py | MODIFY | Validate prompt injection assessment example and updated trace example contract. |

## Contracts touched
- Create `prompt_injection_assessment` schema/example and register in `contracts/standards-manifest.json`.
- Modify `agent_execution_trace` schema/example and bump schema version in `contracts/standards-manifest.json`.

## Tests that must pass after execution
1. `pytest tests/test_prompt_injection_defense.py tests/test_agent_executor.py tests/test_agent_golden_path.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
3. `python scripts/run_contract_enforcement.py`
4. `.codex/skills/contract-boundary-audit/run.sh`
5. `.codex/skills/verify-changed-scope/run.sh`

## Scope exclusions
- Do not redesign retrieval, ranking, or search components.
- Do not redesign routing policy architecture.
- Do not redesign prompt registry or prompt resolution systems.
- Do not introduce fuzzy/probabilistic ML classifier behavior.
- Do not build broad security platform features outside this bounded seam.

## Dependencies
- HS-06 context bundle contract and deterministic composition remain authoritative input boundary.
- HS-03 model adapter boundary remains the canonical model invocation seam.
- AG-01/AG-02 execution flow remains bounded and fail-closed.
