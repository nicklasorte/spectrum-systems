# Plan — AEA-001-FIX — 2026-04-16

## Prompt type
PLAN

## Roadmap item
AEA-001-FIX — registry-overlap repair for prior AI enforcement changes

## Objective
Remove centralized cross-owner enforcement logic and preserve only composition-by-reference so owner-local outputs stay with canonical owners and protected authority remains CDE-only.

## Ownership diagnosis (suspicious files)
- `spectrum_systems/modules/runtime/aea_ai_enforcement_autonomy.py`: centralized recomputation/emission for many owners; must be reduced to composition-only reference handling.
- `contracts/schemas/con_*.schema.json`: must remain compliance evidence only.
- `contracts/schemas/evl_*.schema.json`: must remain eval evidence only.
- `contracts/schemas/evd/lin/rep/obs*.schema.json`: must remain owner-local posture only.
- `contracts/schemas/prm/ctx/prg/qos/cap/slo*.schema.json`: must remain scoped signals/posture only (no final authority).
- `contracts/schemas/cde_*.schema.json`: protected final authority allowed only here.
- `tests/test_aea_ai_enforcement_autonomy.py`: replace centralized behavior assertions with owner-boundary and leak-guard assertions.

## Declared files
| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-AEA-001-FIX-2026-04-16.md | CREATE | Plan-first repair artifact with overlap diagnosis. |
| spectrum_systems/modules/runtime/aea_ai_enforcement_autonomy.py | MODIFY | Remove centralized cross-owner logic; keep composition-only utilities. |
| tests/test_aea_ai_enforcement_autonomy.py | MODIFY | Replace overlap-heavy tests with boundary/leak guard tests. |

## Contracts touched
None (semantic repair is runtime/test composition-only; existing owner-local contract set retained).

## Tests that must pass after execution
1. `python scripts/run_system_registry_guard.py --changed-files ...`
2. `pytest tests/test_aea_ai_enforcement_autonomy.py`
3. `pytest tests/test_contracts.py`
4. `pytest tests/test_contract_enforcement.py`
5. `python scripts/run_contract_enforcement.py`

## Scope exclusions
- Do not add new owners or new broad AI runtime subsystem.
- Do not redesign system registry.
- Do not broaden contract authority semantics.
