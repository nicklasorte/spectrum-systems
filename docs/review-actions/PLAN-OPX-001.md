# PLAN-OPX-001

- Primary prompt type: BUILD
- Date: 2026-04-12

## Intent
Implement an executable OPX runtime surface that serializes slices OPX-00 through OPX-28 as governed artifact-first execution, with deterministic tests and explicit owner boundaries.

## Steps
1. Add an OPX runtime package (`spectrum_systems/opx/`) with governed slice manifest, owner map, artifact schemas, and serial execution engine.
2. Implement operator action layer, review queue, FAQ module path, certification/replay, dataset registry, runbooks, compression tracker, bypass detection, canary/rollback, reuse ledger, error budgets, active-set manager, compatibility and override audits, red-team packs/fix waves, template extraction, additional module paths, control-plane outputs, backtesting, simulation family, prioritizer, and maintain-stage outputs.
3. Add deterministic tests covering the 25 mandatory coverage assertions.
4. Write implementation review report at `docs/reviews/2026-04-12T220000Z_opx_implementation_review.md`.
5. Run targeted test commands and commit.
