# Plan — VAL-07 — 2026-03-28

## Prompt type
PLAN

## Roadmap item
VAL-07 — Eval Auto-Generation Quality (`docs/roadmap/trust_hardening_roadmap.md`)

## Objective
Implement a governed validation slice that deterministically exercises the real eval auto-generation seam and proves generated eval artifacts are meaningful, schema-valid, executable, traceable to real failure patterns, and fail-closed on insufficient/malformed inputs.

## Declared files
List every file that will be created, modified, or deleted.
No other files may be changed during execution of this plan.

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-VAL-07-2026-03-28.md | CREATE | Required plan-first artifact for multi-file VAL-07 validation slice. |
| PLANS.md | MODIFY | Register newly created VAL-07 plan in active plan table per planning rules. |
| contracts/examples/eval_auto_generation_quality_result.json | CREATE | Golden-path governed example for the VAL-07 output artifact. |
| contracts/schemas/eval_auto_generation_quality_result.schema.json | CREATE | Contract-first schema for governed VAL-07 output artifact. |
| contracts/standards-manifest.json | MODIFY | Register new VAL-07 artifact contract and bump manifest version. |
| spectrum_systems/modules/governance/eval_auto_generation_quality.py | CREATE | Implement deterministic validation matrix over real auto-generation seam. |
| spectrum_systems/modules/governance/__init__.py | MODIFY | Export VAL-07 validator/error entrypoints. |
| scripts/run_eval_auto_generation_quality_validation.py | CREATE | CLI runner that emits governed artifact and exits non-zero on FAILED. |
| tests/test_eval_auto_generation_quality.py | CREATE | Deterministic VAL-07 coverage for required positive/negative quality cases. |

## Contracts touched
- Add `eval_auto_generation_quality_result.schema.json` (new contract)
- Update `contracts/standards-manifest.json` (new contract registration + version bump)

## Tests that must pass after execution
1. `pytest tests/test_eval_auto_generation_quality.py`
2. `pytest tests/test_cross_run_intelligence.py tests/test_evaluation_auto_generation.py`
3. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
4. `python scripts/run_contract_enforcement.py`
5. `.codex/skills/contract-boundary-audit/run.sh`
6. `.codex/skills/verify-changed-scope/run.sh`

## Scope exclusions
- Do not modify auto-generation semantics in `evaluation_auto_generation.py` or XRUN thresholds in `cross_run_intelligence.py`.
- Do not add parallel eval generators or independent scoring engines.
- Do not relax fail-closed handling or schema validation.
- Do not perform unrelated module/test/document refactors.

## Dependencies
- XRUN-01 seam in `spectrum_systems/modules/runtime/cross_run_intelligence.py` remains authoritative.
- Eval auto-generation seam in `spectrum_systems/modules/runtime/evaluation_auto_generation.py` remains authoritative.
