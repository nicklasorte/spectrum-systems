# Plan — WFL-01 — 2026-04-27

## Prompt type
PLAN

## Roadmap item
WFL-01

## Objective
Ensure governed contract preflight resolves and enforces canonical PR/push base/head refs in artifact-boundary workflow with deterministic tests.

## Declared files
List every file that will be created, modified, or deleted.
No other files may be changed during execution of this plan.

| File | Change type | Reason |
| --- | --- | --- |
| .github/workflows/artifact-boundary.yml | MODIFY | Run governed-contract-preflight on pull_request and push with shared ref outputs and sufficient checkout history. |
| scripts/run_contract_preflight.py | MODIFY | Classify artifact-boundary workflow and artifact-boundary workflow tests as required preflight evaluation surfaces. |
| docs/governance/preflight_required_surface_test_overrides.json | MODIFY | Declare deterministic required pytest targets for artifact-boundary workflow preflight evidence. |
| tests/test_artifact_boundary_workflow_policy_observation.py | CREATE | Add policy observation tests for PR/push ref derivation, checkout depth, and shared wrapper/runner ref wiring. |

## Contracts touched
None.

## Tests that must pass after execution
1. `pytest -q tests/test_artifact_boundary_workflow_policy_observation.py`
2. `pytest -q tests/test_build_preflight_pqx_wrapper.py`
3. `pytest -q tests/test_contract_preflight.py tests/test_preflight_ref_normalization.py tests/test_changed_path_resolution.py`
4. `python scripts/run_authority_shape_preflight.py`

## Scope exclusions
- Do not modify `scripts/build_preflight_pqx_wrapper.py` behavior unless required by test or workflow mismatch.
- Do not alter unrelated workflows or required check policy files.
- Do not refactor preflight runtime modules outside this workflow-bound fix.

## Dependencies
- None.
