# Plan — BAZ — 2026-03-21

## Prompt type
PLAN

## Roadmap item
Prompt BAZ — Agent Execution Module

## Objective
Implement a bounded, schema-governed agent execution surface that consumes a context bundle, executes a deterministic step sequence, and emits a replay-compatible execution trace artifact.

## Declared files
List every file that will be created, modified, or deleted.
No other files may be changed during execution of this plan.

| File | Change type | Reason |
| --- | --- | --- |
| contracts/schemas/agent_execution_trace.schema.json | CREATE | Define governed execution trace contract for BAZ artifacts |
| contracts/examples/agent_execution_trace.json | CREATE | Add golden-path contract example for validation workflows |
| contracts/standards-manifest.json | MODIFY | Register and version-pin the new contract in the canonical manifest |
| spectrum_systems/modules/agents/__init__.py | CREATE | Expose bounded agent execution module API |
| spectrum_systems/modules/agents/agent_executor.py | CREATE | Implement deterministic bounded execution surface and validation functions |
| tests/test_agent_executor.py | CREATE | Add deterministic unit tests for success/failure/blocked/invalid-output scenarios |
| tests/test_contracts.py | MODIFY | Add new contract to canonical schema/example validation list |
| PLANS.md | MODIFY | Register active BAZ plan in plan index |

## Contracts touched
- CREATE `contracts/schemas/agent_execution_trace.schema.json`
- MODIFY `contracts/standards-manifest.json` (version bump + contract registration)

## Tests that must pass after execution
1. `pytest tests/test_agent_executor.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
3. `python scripts/run_contract_enforcement.py`
4. `pytest tests/test_module_architecture.py`

## Scope exclusions
Explicitly list things that are NOT in scope for this plan.

- Do not modify control-chain decision logic.
- Do not add evaluation-driven control decisions.
- Do not add autonomous loops, recursive self-calls, or self-judging behavior.
- Do not add new ontology/taxonomy classes.

## Dependencies
List any prior roadmap items that must be complete before this plan can execute.

- BAG (Replay Engine MVP) for replay-compatibility assumptions.
- BN.6 (Control Signal Consumption Layer) remains unchanged and out of scope.
