# Plan — HS-03 — 2026-03-24

## Prompt type
PLAN

## Roadmap item
HS-03 — Model Adapter Layer

## Objective
Introduce a strict canonical model adapter boundary so AG runtime execution consumes governed ai_model_request/ai_model_response artifacts with deterministic linkage to prompt resolution and execution trace.

## Declared files
List every file that will be created, modified, or deleted.
No other files may be changed during execution of this plan.

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-HS-03-2026-03-24.md | CREATE | Required PLAN artifact before multi-file BUILD scope. |
| PLANS.md | MODIFY | Register active HS-03 plan in active plans table. |
| contracts/schemas/ai_model_request.schema.json | CREATE | Add canonical governed request contract for model adapter boundary. |
| contracts/schemas/ai_model_response.schema.json | CREATE | Add canonical governed response contract for model adapter boundary. |
| contracts/examples/ai_model_request.json | CREATE | Golden-path canonical request example. |
| contracts/examples/ai_model_response.json | CREATE | Golden-path canonical response example. |
| contracts/schemas/agent_execution_trace.schema.json | MODIFY | Add strict model linkage surface in execution trace schema. |
| contracts/examples/agent_execution_trace.json | MODIFY | Keep example aligned with execution trace schema updates. |
| contracts/standards-manifest.json | MODIFY | Register new contracts and bump manifest metadata/version fields. |
| spectrum_systems/modules/runtime/model_adapter.py | CREATE | Implement canonical request construction and provider response normalization fail-closed boundary. |
| spectrum_systems/modules/agents/agent_executor.py | MODIFY | Integrate model adapter seam into bounded AG execution and trace emission. |
| spectrum_systems/modules/runtime/agent_golden_path.py | MODIFY | Wire HS-03 adapter at the existing AG runtime model execution seam. |
| docs/runtime/model_adapter.md | CREATE | Focused HS-03 documentation for contracts, boundary rules, trace linkage, and out-of-scope constraints. |
| tests/test_contracts.py | MODIFY | Add ai_model_request/ai_model_response schema+example validation coverage. |
| tests/test_agent_executor.py | MODIFY | Add runtime model linkage and fail-closed adapter boundary coverage. |
| tests/test_agent_golden_path.py | MODIFY | Verify runtime trace contains prompt + model linkage in AG golden-path execution. |
| tests/test_model_adapter.py | CREATE | Add focused unit tests for deterministic request construction and response normalization/rejection behavior. |

## Contracts touched
- Create `ai_model_request` schema/example and register in `contracts/standards-manifest.json`.
- Create `ai_model_response` schema/example and register in `contracts/standards-manifest.json`.
- Modify `agent_execution_trace` schema/example to include model linkage fields.

## Tests that must pass after execution
1. `pytest tests/test_model_adapter.py tests/test_agent_executor.py tests/test_agent_golden_path.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
3. `python scripts/run_contract_enforcement.py`
4. `.codex/skills/contract-boundary-audit/run.sh`
5. `.codex/skills/verify-changed-scope/run.sh`

## Scope exclusions
- Do not implement provider routing policy.
- Do not implement fallback orchestration across providers.
- Do not add UI/CLI flows beyond narrow AG runtime seam integration.
- Do not perform performance optimization work.
- Do not build a broad plugin framework or multi-provider abstraction beyond current effective provider path.

## Dependencies
- HS-01 prompt registry resolution and runtime prompt linkage must remain compatible.
