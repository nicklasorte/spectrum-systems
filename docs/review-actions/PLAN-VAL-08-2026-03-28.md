# Plan — VAL-08 — 2026-03-28

## Prompt type
PLAN

## Roadmap item
VAL-08 — End-to-End Failure Simulation

## Objective
Implement a deterministic end-to-end failure simulation matrix that composes real governed seams and emits a governed validation artifact that fails closed on silent propagation, missing failure artifacts, and blocking breakdowns.

## Declared files

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-VAL-08-2026-03-28.md | CREATE | Required plan-first artifact for multi-file/new schema/new module scope. |
| spectrum_systems/modules/governance/end_to_end_failure_simulation.py | CREATE | New VAL-08 orchestration module using real runtime/governance seams. |
| contracts/schemas/end_to_end_failure_simulation_result.schema.json | CREATE | Contract-first schema for VAL-08 result artifact. |
| contracts/examples/end_to_end_failure_simulation_result.json | CREATE | Canonical deterministic example payload for the new schema. |
| contracts/standards-manifest.json | MODIFY | Register new contract artifact and version metadata. |
| scripts/run_end_to_end_failure_simulation.py | CREATE | CLI runner for governed input refs and result output/exit semantics. |
| tests/test_end_to_end_failure_simulation.py | CREATE | Deterministic validation coverage for required VAL-08 cases and failure flags. |

## Contracts touched
- Create `end_to_end_failure_simulation_result` schema version `1.0.0` and add to `contracts/standards-manifest.json`.

## Tests that must pass after execution
1. `pytest tests/test_end_to_end_failure_simulation.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
3. `python scripts/run_contract_enforcement.py`
4. `.codex/skills/verify-changed-scope/run.sh`

## Scope exclusions
- Do not modify existing runtime seam logic in replay/eval/control/enforcement/certification modules.
- Do not introduce probabilistic logic or random fixtures.
- Do not add any new repository/module boundaries beyond VAL-08 artifacts.

## Dependencies
- Existing governed seams from AG-01/BAG/BBA/BAF/DONE-01/ADV-01/XRUN-01 must remain intact and are reused directly.
