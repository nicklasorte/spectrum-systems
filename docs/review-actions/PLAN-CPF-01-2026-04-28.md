# Plan — CPF-01 — 2026-04-28

## Prompt type
BUILD

## Roadmap item
CPF-01

## Objective
Reproduce PR 1268 contract preflight failure, identify the internal preflight error root cause, and implement a minimal fail-closed fix that yields precise governed classification.

## Declared files

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-CPF-01-2026-04-28.md | CREATE | Plan-first requirement for multi-file execution. |
| scripts/run_contract_preflight.py | MODIFY (if needed) | Fix thin CLI/runner boundary handling causing internal preflight error. |
| scripts/build_preflight_pqx_wrapper.py | MODIFY (if needed) | Fix wrapper generation/validation mismatch if root cause is wrapper shape/content. |
| spectrum_systems/**/contract_preflight*.py | MODIFY (if needed) | Fix preflight evaluation/classification path without weakening fail-closed behavior. |
| spectrum_systems/**/preflight*.py | MODIFY (if needed) | Fix invariant breach in preflight helper(s). |
| spectrum_systems/**/pqx*.py | MODIFY (if needed) | Fix PQX wrapper or authority evidence loading boundary. |
| tests/** | MODIFY/CREATE (if needed) | Add regression coverage for discovered root cause and governed classification behavior. |

## Contracts touched
None expected. If schema/manifest mismatch is root cause, update associated schema/example/manifest together in same change.

## Tests that must pass after execution
1. `python scripts/build_preflight_pqx_wrapper.py --base-ref 0fac70bd2179e08edb8ddfcf8f8c9f6716775dd1 --head-ref 08f633f8a8b6b8eaca63bcef9560b072e90878f7 --output outputs/contract_preflight/preflight_pqx_task_wrapper.json`
2. `python scripts/run_contract_preflight.py --base-ref 0fac70bd2179e08edb8ddfcf8f8c9f6716775dd1 --head-ref 08f633f8a8b6b8eaca63bcef9560b072e90878f7 --output-dir outputs/contract_preflight --execution-context pqx_governed --pqx-wrapper-path outputs/contract_preflight/preflight_pqx_task_wrapper.json --authority-evidence-ref artifacts/pqx_runs/preflight.pqx_slice_execution_record.json`
3. Relevant pytest targets for contract preflight, PQX wrapper generation, authority evidence validation, and block bundle classification/escalation paths.

## Scope exclusions
- Do not weaken fail-closed gates.
- Do not add warning-only fallbacks for internal errors.
- Do not bypass PQX-governed execution context or authority evidence requirements.
- Do not perform unrelated UI/dashboard refactors.

## Dependencies
None declared.
