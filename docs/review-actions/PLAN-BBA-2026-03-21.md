# Plan — BBA Eval → Control Loop Integration — 2026-03-21

## Prompt type
PLAN

## Roadmap item
BBA — Eval → Control Loop Integration

## Objective
Integrate `eval_summary` artifacts into the existing runtime control path by producing a governed `evaluation_control_decision` artifact and enforcing fail-closed continuation behavior through existing control integration surfaces.

## Declared files

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-BBA-2026-03-21.md | CREATE | Required plan-first artifact for multi-file schema + runtime integration changes. |
| PLANS.md | MODIFY | Register this plan in active plans table. |
| contracts/schemas/evaluation_control_decision.schema.json | CREATE | Required new governed contract for eval-to-control decision artifact. |
| contracts/examples/evaluation_control_decision.json | CREATE | Golden-path example payload for the new contract. |
| contracts/standards-manifest.json | MODIFY | Register new contract and bump manifest version metadata. |
| spectrum_systems/modules/runtime/evaluation_control.py | CREATE | Deterministic eval-summary-to-control decision mapper with schema validation and fail-closed behavior. |
| spectrum_systems/modules/runtime/control_integration.py | MODIFY | Integrate eval-control decision into existing execution gate without creating a parallel enforcement path. |
| tests/test_evaluation_control.py | CREATE | Deterministic unit tests for decision mapping and fail-closed behavior. |
| tests/test_control_integration.py | MODIFY | Integration tests asserting eval_summary artifacts are consumed and non-allow outcomes block continuation. |

## Contracts touched
- Create `evaluation_control_decision` (`1.0.0`) in `contracts/schemas/evaluation_control_decision.schema.json`.
- Update `contracts/standards-manifest.json` with new contract registration and manifest artifact version bump.

## Tests that must pass after execution
1. `pytest tests/test_evaluation_control.py tests/test_control_integration.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
3. `python scripts/run_contract_enforcement.py`
4. `.codex/skills/contract-boundary-audit/run.sh`
5. `pytest`

## Scope exclusions
- Do not modify BAY or BAZ schema contracts beyond compatibility with consuming `eval_summary`.
- Do not introduce human override logic for evaluation control decisions.
- Do not add autonomous policy reasoning or heuristic/non-deterministic control logic.
- Do not alter agent execution behavior.

## Dependencies
- Existing BN control-chain and BN.7 control-integration modules must remain the authoritative execution gate.
- Existing BAY `eval_summary` contract and producer behavior must remain compatible.
