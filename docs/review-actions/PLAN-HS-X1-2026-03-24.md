# Plan — HS-X1 — 2026-03-24

## Prompt type
PLAN

## Roadmap item
HS-X1 — Structured Decoding + Schema-Constrained Generation

## Objective
Harden canonical model generation so structured steps must declare and bind to an explicit governed target schema with fail-closed enforcement at the model adapter boundary and deterministic trace linkage.

## Declared files
List every file that will be created, modified, or deleted.
No other files may be changed during execution of this plan.

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-HS-X1-2026-03-24.md | CREATE | Required PLAN artifact before multi-file BUILD scope. |
| PLANS.md | MODIFY | Register active HS-X1 plan in active plans table. |
| contracts/schemas/ai_model_request.schema.json | MODIFY | Extend canonical request contract with structured generation semantics and target schema binding. |
| contracts/schemas/ai_model_response.schema.json | MODIFY | Extend canonical response contract with structured generation outcome/linkage status. |
| contracts/schemas/agent_execution_trace.schema.json | MODIFY | Extend model invocation trace linkage with structured generation fields. |
| contracts/examples/ai_model_request.json | MODIFY | Keep request example aligned with schema-constrained request shape. |
| contracts/examples/ai_model_response.json | MODIFY | Keep response example aligned with structured generation result shape. |
| contracts/examples/agent_execution_trace.json | MODIFY | Keep trace example aligned with structured invocation linkage shape. |
| contracts/standards-manifest.json | MODIFY | Version bump and update touched contract entries. |
| spectrum_systems/modules/runtime/model_adapter.py | MODIFY | Add structured generation request/response handling and provider-boundary fail-closed enforcement. |
| spectrum_systems/modules/agents/agent_executor.py | MODIFY | Wire structured generation declaration and linkage capture at AG runtime/model seam. |
| docs/runtime/structured_generation.md | CREATE | Focused HS-X1 semantics, enforcement, traceability, fail-closed, and scope notes. |
| tests/test_model_adapter.py | MODIFY | Add deterministic structured-request construction and provider boundary rejection coverage. |
| tests/test_agent_executor.py | MODIFY | Add runtime fail-closed and trace linkage coverage for structured steps. |
| tests/test_agent_golden_path.py | MODIFY | Verify structured generation linkage appears in runtime trace and no silent downgrade behavior. |
| tests/test_contracts.py | MODIFY | Ensure contract examples validate after schema updates. |

## Contracts touched
- Modify `ai_model_request` schema/example and bump schema version in `contracts/standards-manifest.json`.
- Modify `ai_model_response` schema/example and bump schema version in `contracts/standards-manifest.json`.
- Modify `agent_execution_trace` schema/example and bump schema version in `contracts/standards-manifest.json`.

## Tests that must pass after execution
1. `pytest tests/test_model_adapter.py tests/test_agent_executor.py tests/test_agent_golden_path.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
3. `python scripts/run_contract_enforcement.py`
4. `.codex/skills/contract-boundary-audit/run.sh`
5. `.codex/skills/verify-changed-scope/run.sh`

## Scope exclusions
- Do not redesign routing policy selection behavior.
- Do not add fallback orchestration across providers.
- Do not add broad multi-provider abstraction expansion.
- Do not redesign streaming architecture.
- Do not redesign prompt authoring surfaces.

## Dependencies
- HS-03 canonical model adapter boundary must remain the single provider seam.
- HS-04 routing decisions and prompt resolution linkage must remain compatible.
