# PLAN — SS-HARD-03 (2026-04-08)

## Prompt Type
BUILD

## Scope
Narrow hardening slice to:
1. Unify governed enforcement execution to one canonical path/artifact.
2. Add fail-closed queue/bundle state consistency checks at continuation/resume boundaries.
3. Add targeted regressions to preserve SS-HARD-01 authority and SS-HARD-02 execution-mode truth on touched paths.

## Steps
1. Inspect current runtime enforcement entrypoints and governed callers; identify legacy budget path usage.
2. Migrate governed execution callers/consumers to a single canonical enforcement artifact shape and entrypoint; prevent governed legacy-path invocation.
3. Inspect prompt-queue and PQX bundle state progression/resume logic; add fail-closed consistency assertion with structured divergence signaling.
4. Add/adjust tests for canonical enforcement singularity, governed legacy-path closure, queue/bundle divergence fail-closed behavior, and resume-token mismatch rejection.
5. Run targeted module + orchestration tests; verify no SS-HARD-01/02 regression in touched paths.
