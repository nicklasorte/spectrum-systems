# Plan — SRE-09 — 2026-03-24

## Prompt type
PLAN

## Roadmap item
SRE-09 — Error Budget Model

## Objective
Publish a governed error-budget contract and deterministic runtime computation path that consumes existing SLO + observability artifacts and attaches an `error_budget_status` artifact to the authoritative replay seam.

## Declared files

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-SRE-09-2026-03-24.md | CREATE | Required plan artifact for SRE-09 multi-file contract/runtime work. |
| contracts/schemas/error_budget_policy.schema.json | CREATE | Governed error budget policy contract. |
| contracts/examples/error_budget_policy.json | CREATE | Golden-path policy example. |
| contracts/schemas/error_budget_status.schema.json | CREATE | Governed error budget status contract. |
| contracts/examples/error_budget_status.json | CREATE | Golden-path status example. |
| data/policy/error_budget_policy.json | CREATE | Deterministic runtime-loadable policy artifact for budget computation. |
| contracts/schemas/replay_result.schema.json | MODIFY | Add optional embedded `error_budget_status` object at the replay authoritative seam. |
| contracts/standards-manifest.json | MODIFY | Register new contracts and bump manifest publication metadata/version. |
| spectrum_systems/modules/runtime/error_budget.py | MODIFY | Replace legacy rolling tracker with deterministic schema-governed status builder and policy loader. |
| spectrum_systems/modules/runtime/replay_engine.py | MODIFY | Build/attach error budget artifact in replay flow when SLO input is supplied. |
| scripts/run_error_budget.py | CREATE | Thin deterministic CLI for error budget artifact generation. |
| tests/test_error_budget.py | MODIFY | Focused deterministic/fail-closed unit coverage for SRE-09 semantics. |
| tests/test_replay_engine.py | MODIFY | Assert replay seam attachment of `error_budget_status` and linkage parity. |
| tests/test_contracts.py | MODIFY | Contract example validation for new error-budget artifacts. |
| docs/runtime/error_budget.md | CREATE | Runtime semantics, fail-closed rules, and seam rationale for SRE-09. |

## Contracts touched
- `contracts/schemas/error_budget_policy.schema.json` (new)
- `contracts/schemas/error_budget_status.schema.json` (new)
- `contracts/schemas/replay_result.schema.json` (updated)
- `contracts/standards-manifest.json` (version and registration update)

## Tests that must pass after execution
1. `pytest tests/test_error_budget.py tests/test_replay_engine.py tests/test_contracts.py tests/test_contract_enforcement.py`
2. `python scripts/run_contract_enforcement.py`
3. `.codex/skills/contract-boundary-audit/run.sh`
4. `.codex/skills/verify-changed-scope/run.sh`

## Scope exclusions
- Do not add alerting, notification, or dashboard behavior.
- Do not add budget-based enforcement/gating actions.
- Do not redesign observability metric computation surface.
- Do not add non-deterministic IDs/timestamps to budget artifacts.

## Dependencies
- SRE-08 (`service_level_objective`) must remain the canonical objective contract.
- SRE-10 (`observability_metrics`) must remain the canonical deterministic measurement input.
