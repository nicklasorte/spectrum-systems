# Plan — MAP-001 + MAP-002 — 2026-04-03

## Prompt type
PLAN

## Roadmap item
MAP-001 — Review Artifact Foundation; MAP-002 — Review → Eval → Control Wiring

## Objective
Establish a governed `repo_review_snapshot` artifact and deterministic review-health evaluation/control wiring that converts review outputs into fail-closed control signals through existing eval/control seams.

## Declared files
List every file that will be created, modified, or deleted.
No other files may be changed during execution of this plan.

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-MAP-001-MAP-002-2026-04-03.md | CREATE | Required plan-first artifact for MAP execution. |
| PLANS.md | MODIFY | Register MAP plan in active plan tracking table. |
| contracts/schemas/repo_review_snapshot.schema.json | CREATE | Define governed review snapshot contract (MAP-001). |
| contracts/examples/repo_review_snapshot.json | CREATE | Provide golden-path fixture/example for snapshot contract validation. |
| contracts/standards-manifest.json | MODIFY | Register `repo_review_snapshot` contract in canonical standards registry. |
| spectrum_systems/modules/runtime/repo_review_snapshot_store.py | CREATE | Add deterministic storage/read/validate helper for snapshot artifacts. |
| spectrum_systems/modules/runtime/repo_health_eval.py | CREATE | Implement deterministic review health metric derivation, eval emission, and control seam adapter. |
| tests/test_repo_review_snapshot_store.py | CREATE | Validate schema + storage round-trip + fail-closed behavior for snapshot artifacts. |
| tests/test_repo_health_eval.py | CREATE | Validate deterministic metric/eval emission and pass/warn/freeze/block control interpretation including fail-closed paths. |

## Contracts touched
- `contracts/schemas/repo_review_snapshot.schema.json` (new)
- `contracts/standards-manifest.json` (new registered contract entry)

## Tests that must pass after execution
1. `pytest tests/test_repo_review_snapshot_store.py`
2. `pytest tests/test_repo_health_eval.py`
3. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
4. `python scripts/run_contract_enforcement.py`
5. `python scripts/validate_review_artifacts.py`

## Scope exclusions
- Do not introduce any parallel artifact framework outside repo-native contracts/examples/runtime modules.
- Do not add model/judge scoring to review health evaluation (deterministic thresholds only).
- Do not redesign existing control-loop architecture or replace `evaluation_control` authority paths.

## Dependencies
- Existing `review_control_signal` + `evaluation_control_decision` seams must remain authoritative and unchanged in role.
- Existing contract loader/validator (`spectrum_systems.contracts`) must continue to be the canonical schema enforcement path.
