# Plan — PYX-03 — 2026-04-14

## Prompt type
BUILD

## Scope
Diagnose why the governed pytest authority path is not surfacing as a distinct PR-visible check and apply the smallest workflow/policy/test fix to make the surfaced check explicit and stable without weakening fail-closed trust enforcement.

## Files in scope
| File | Action | Reason |
| --- | --- | --- |
| docs/reviews/PYX-03_pr_check_surface_diagnosis.md | CREATE | Record check-surface diagnosis and exact root cause before code changes. |
| .github/workflows/artifact-boundary.yml | MODIFY | Remove pytest authority job from mixed workflow so surfaced status is no longer hidden behind generic workflow prefix. |
| .github/workflows/pr-pytest.yml | CREATE | Host the authoritative governed preflight/pytest path in a dedicated PR-visible workflow with stable surfaced check context. |
| docs/governance/required_pr_checks.json | MODIFY | Align governed required-check policy to the real surfaced check context name. |
| scripts/run_required_check_alignment_audit.py | MODIFY | Parse and audit the new authoritative workflow/job naming surface. |
| spectrum_systems/modules/runtime/required_check_alignment_audit.py | MODIFY | Audit expected required check against surfaced workflow/job composite check name. |
| tests/test_artifact_boundary_workflow_pytest_enforcement.py | MODIFY | Validate authoritative preflight trust gate moved to dedicated workflow and remains strict/fail-closed. |
| tests/test_required_check_alignment_audit.py | MODIFY | Assert policy/workflow/check alignment against new surfaced check context name. |

## Guardrails
- Preserve canonical governed preflight execution and artifact trust checks exactly.
- Preserve fail-closed semantics and ALLOW-only PR pass behavior.
- Avoid duplicate or weaker pytest execution paths.
- Keep artifact-boundary, module boundary, orchestration boundary, and SRG checks intact.
