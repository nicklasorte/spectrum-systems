# PLAN — RAX SERIAL BUILD (2026-04-13)

## Prompt type
BUILD

## Scope
Implement the next governed operating-substrate increments in repo-native surfaces for CTX, TLX, JSX, DRX plus hardening/wiring around TAX/BAX/CAX, promotion readiness, and downstream intake guardrails.

## Serial execution plan
1. Confirm canonical registry and contract authority locations.
2. Add/align runtime modules (CTX, TLX, JSX, DRX, task/prompt/route/adapter/eval substrate, artifact intelligence, readiness/intake guards).
3. Add strict JSON Schema contracts and examples for all new artifact families requested in this phase.
4. Update canonical registry surfaces (`docs/architecture/system_registry.md` and `contracts/examples/system_registry_artifact.json`).
5. Update `contracts/standards-manifest.json` with version bump and new contract entries.
6. Add deterministic tests for runtime logic and contract validation.
7. Add phase review artifacts (phase1–phase16) and final delivery report.
8. Run targeted test/validation commands and commit.
