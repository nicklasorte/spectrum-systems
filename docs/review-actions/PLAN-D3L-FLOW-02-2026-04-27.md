# Plan — D3L-FLOW-02 — 2026-04-27

## Prompt type
BUILD

## Roadmap item
D3L-FLOW-02

## Objective
Upgrade the dashboard’s governed loop visualization into a trust-aware, artifact-backed system flow graph with explicit overlays, filters, edge typing, and node drill-down details while preserving fail-closed behavior.

## Declared files

| File | Change type | Reason |
| --- | --- | --- |
| apps/dashboard-3ls/app/page.tsx | MODIFY | Add graph rendering, node interaction, overlays/filters, legend, and fail-closed graph-state handling. |
| apps/dashboard-3ls/app/api/system-flow/route.ts | CREATE | Expose canonical system graph artifact to UI with explicit missing/invalid fail states. |
| apps/dashboard-3ls/__tests__/components/DashboardPage.test.tsx | MODIFY | Add/adjust tests for graph render, trust colors, fallback nodes, and broken edges visibility. |
| PLANS.md | MODIFY | Register this plan in active plans per repository planning policy. |

## Contracts touched
None.

## Tests that must pass after execution
1. `npm test -- --runInBand __tests__/components/DashboardPage.test.tsx` (from `apps/dashboard-3ls`)

## Scope exclusions
- Do not change priority ranking logic or ranking artifact schema.
- Do not modify canonical system ownership definitions in architecture registry docs.
- Do not introduce hardcoded relationship edges in UI logic.

## Dependencies
- Existing TLS artifacts (`artifacts/tls/system_registry_dependency_graph.json`, `artifacts/system_dependency_priority_report.json`) must remain available and schema-compatible.
