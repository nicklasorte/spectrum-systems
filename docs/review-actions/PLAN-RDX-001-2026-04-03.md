# Plan — RDX-001 — 2026-04-03

## Prompt type
PLAN

## Roadmap item
RDX-001 — BATCH-M: Roadmap Artifact Foundation

## Objective
Introduce a strict, schema-validated roadmap artifact contract and deterministic storage/validation layer that records batch execution state for future governed execution.

## Declared files
List every file that will be created, modified, or deleted.
No other files may be changed during execution of this plan.

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-RDX-001-2026-04-03.md | CREATE | Required plan-first artifact for this multi-file contract + runtime change |
| PLANS.md | MODIFY | Register this plan in the active plans table |
| contracts/schemas/roadmap_artifact.schema.json | CREATE | Canonical governed roadmap artifact schema with strict typing and fail-closed structure |
| contracts/examples/roadmap_artifact.json | CREATE | Golden-path example instance representing BATCH-A..BATCH-M state |
| contracts/standards-manifest.json | MODIFY | Register new roadmap_artifact contract and example path with version update |
| spectrum_systems/modules/runtime/roadmap_artifact_store.py | CREATE | Deterministic read/write/validate storage helpers for roadmap artifacts |
| tests/test_roadmap_artifact_store.py | CREATE | Deterministic validation tests for schema, fail-closed behavior, round-trip I/O, and ordering |

## Contracts touched
- Create `roadmap_artifact` schema at `contracts/schemas/roadmap_artifact.schema.json`.
- Register `roadmap_artifact` in `contracts/standards-manifest.json`.

## Tests that must pass after execution
1. `pytest tests/test_roadmap_artifact_store.py`
2. `pytest tests/test_contracts.py`
3. `pytest tests/test_contract_enforcement.py`
4. `python scripts/run_contract_enforcement.py`

## Scope exclusions
- Do not implement roadmap execution logic, batch selection logic, or scheduling logic.
- Do not integrate roadmap artifact consumption into PQX in this slice.
- Do not refactor unrelated modules or existing contract artifacts.

## Dependencies
- Existing roadmap authority and contract-loading infrastructure in `spectrum_systems/contracts/`.
