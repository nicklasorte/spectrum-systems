# Plan — MET-01-02 — 2026-04-25

## Prompt type
PLAN

## Roadmap item
MET-01-02 — Seed Real Artifacts + Wire Minimal Governed Loop

## Objective
Seed a minimal artifact-backed governed loop and wire dashboard APIs/loaders so dashboard truth moves from pure fallback to partial artifact-backed warning without fake PASS.

## Declared files

| File | Change type | Reason |
| --- | --- | --- |
| artifacts/dashboard_seed/*.json | CREATE | Seed minimal real artifact records and loop snapshot |
| apps/dashboard-3ls/lib/types.ts | MODIFY | Add minimal seed artifact types used by routes/tests |
| apps/dashboard-3ls/lib/systemSignals.ts | MODIFY | Surface seed-backed loop and fallback counts |
| apps/dashboard-3ls/lib/rgeSignals.ts | MODIFY | Add seed-derived warning semantics where available |
| apps/dashboard-3ls/app/api/intelligence/route.ts | MODIFY | Include seed loop coverage/failure mode/near miss summaries |
| apps/dashboard-3ls/app/api/health/route.ts | MODIFY | Blend seed artifact-backed statuses into system cards without fake green |
| apps/dashboard-3ls/app/api/debug/artifacts/route.ts | CREATE or MODIFY | Report dashboard_seed discovery and expected file gaps |
| apps/dashboard-3ls/next.config.js | MODIFY (if needed) | Ensure artifact tracing includes dashboard_seed files |
| apps/dashboard-3ls/__tests__/api/*.test.ts | MODIFY | Validate seed artifact loading, fallback behavior, and no fake pass |
| docs/reviews/MET-01-02-dashboard-seed-loop.md | CREATE | Review artifact documenting seeded loop and residual gaps |

## Contracts touched
None (no schema changes; seed artifacts follow existing envelope conventions).

## Tests that must pass after execution
1. `cd apps/dashboard-3ls && npm run build`
2. `cd apps/dashboard-3ls && npm run test`
3. `pytest`

## Scope exclusions
- Do not weaken dashboard truth rules or no-green-without-source behavior.
- Do not remove fallback warnings.
- Do not implement a full execution engine beyond the minimal seed loop.
- Do not modify canonical ownership definitions.

## Dependencies
- Existing dashboard truth-layer routes and classifiers from DSH-09/DSH-11 must remain authoritative.
