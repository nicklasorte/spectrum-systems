# Plan — VAL-06 — 2026-03-28

## Prompt type
PLAN

## Roadmap item
VAL-06 — XRUN Signal Quality (`docs/roadmap/trust_hardening_roadmap.md`)

## Objective
Implement a governed validation slice that deterministically exercises the real XRUN-01 seam and proves pattern/action/signal quality plus fail-closed behavior on insufficient and malformed inputs.

## Declared files
List every file that will be created, modified, or deleted.
No other files may be changed during execution of this plan.

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-VAL-06-2026-03-28.md | CREATE | Required plan-first artifact for multi-file VALIDATE slice. |
| PLANS.md | MODIFY | Register newly created VAL-06 plan in active plan table per repository planning rules. |
| contracts/examples/xrun_signal_quality_result.json | CREATE | Provide governed golden-path example for the new VAL-06 result contract. |
| contracts/schemas/xrun_signal_quality_result.schema.json | CREATE | Governed artifact schema for VAL-06 output. |
| contracts/standards-manifest.json | MODIFY | Register new VAL-06 artifact contract and bump manifest version. |
| spectrum_systems/modules/governance/xrun_signal_quality.py | CREATE | Implement deterministic VAL-06 validation matrix over real XRUN seam. |
| spectrum_systems/modules/governance/__init__.py | MODIFY | Export VAL-06 validator entrypoint/error class. |
| scripts/run_xrun_signal_quality_validation.py | CREATE | CLI runner to execute validation and emit governed artifact with non-zero exit on FAILED. |
| tests/test_xrun_signal_quality.py | CREATE | Deterministic coverage for required VAL-06 cases and failure criteria. |

## Contracts touched
- Add `xrun_signal_quality_result.schema.json` (new contract)
- Update `contracts/standards-manifest.json` (new contract registration + manifest version bump)

## Tests that must pass after execution
1. `pytest tests/test_xrun_signal_quality.py`
2. `pytest tests/test_cross_run_intelligence.py tests/test_evaluation_auto_generation.py`
3. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
4. `python scripts/run_contract_enforcement.py`
5. `.codex/skills/contract-boundary-audit/run.sh`
6. `.codex/skills/verify-changed-scope/run.sh`

## Scope exclusions
- Do not modify XRUN core pattern thresholds/semantics in `cross_run_intelligence.py`.
- Do not add any new recommendation engine or learning loop.
- Do not change unrelated governance modules, runtime modules, or roadmap files.
- Do not relax schema validation or fail-closed behavior.

## Dependencies
- XRUN-01 seam implementation in `spectrum_systems/modules/runtime/cross_run_intelligence.py` must remain authoritative.
- Existing eval auto-generation path in `spectrum_systems/modules/runtime/evaluation_auto_generation.py` is reused for generated eval-case checks.
