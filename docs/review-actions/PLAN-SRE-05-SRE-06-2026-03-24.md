# Plan — SRE-05 + SRE-06 Drift Detection + Baseline Gating Hardening — 2026-03-24

## Prompt type
PLAN

## Roadmap item
SRE-05 + SRE-06

## Objective
Implement deterministic, fail-closed baseline drift detection and baseline gate decision artifacts, then wire gating enforcement into governed replay runtime flow.

## Declared files

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-SRE-05-SRE-06-2026-03-24.md | CREATE | Required PLAN artifact for this multi-file contract/runtime slice. |
| contracts/schemas/drift_detection_result.schema.json | MODIFY | Harden to strict deterministic drift artifact contract. |
| contracts/examples/drift_detection_result.json | CREATE | Golden-path example for hardened drift detection contract. |
| contracts/schemas/baseline_gate_decision.schema.json | CREATE | Governed fail-closed baseline gate decision contract. |
| contracts/examples/baseline_gate_decision.json | CREATE | Golden-path example for baseline gate decision contract. |
| contracts/schemas/baseline_gate_policy.schema.json | CREATE | Governed policy contract for drift threshold and gating behavior. |
| contracts/examples/baseline_gate_policy.json | CREATE | Golden-path example for baseline gate policy contract. |
| contracts/standards-manifest.json | MODIFY | Register/refresh schema versions for touched contracts. |
| contracts/schemas/replay_result.schema.json | MODIFY | Allow governed replay artifact to carry drift detection and baseline gate decision attachments. |
| data/policy/baseline_gate_policy.json | CREATE | Canonical runtime-loaded baseline gate policy instance. |
| spectrum_systems/modules/runtime/drift_detection.py | CREATE | Deterministic drift comparison module with schema enforcement. |
| spectrum_systems/modules/runtime/baseline_gating.py | CREATE | Deterministic baseline gate decision module with schema enforcement. |
| spectrum_systems/modules/runtime/replay_engine.py | MODIFY | Integrate drift + baseline gate artifacts and enforce block decisions fail-closed. |
| scripts/run_drift_detection.py | MODIFY | CLI wiring to new drift detection module and status vocabulary. |
| tests/test_drift_detection.py | CREATE | Focused deterministic drift detection tests. |
| tests/test_baseline_gating.py | CREATE | Focused baseline gating tests. |
| tests/test_replay_engine.py | MODIFY | Integration tests for baseline gate attachment and blocking behavior. |
| tests/test_contracts.py | MODIFY | Validate new contract examples in standard contract test suite. |
| docs/runtime/drift_detection.md | CREATE | Runtime docs for drift semantics and policy linkage. |
| docs/runtime/baseline_gating.md | CREATE | Runtime docs for baseline gate semantics and enforcement boundary. |

## Contracts touched
- drift_detection_result (schema hardening, example added)
- baseline_gate_decision (new schema + example)
- baseline_gate_policy (new schema + example)
- standards manifest entry updates for contract registration/version bumps

## Tests that must pass after execution
1. `pytest tests/test_drift_detection.py tests/test_baseline_gating.py tests/test_replay_engine.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
3. `python scripts/run_contract_enforcement.py`
4. `.codex/skills/verify-changed-scope/run.sh` with PLAN_FILES set to declared files.

## Scope exclusions
- No UI/dashboard/alerting work.
- No multi-run aggregation redesign.
- No policy simulation redesign beyond baseline-gate policy consumption.
- No unrelated refactors outside listed files.

## Dependencies
- Existing replay_result, drift_result, and replay governance contracts/modules remain the upstream substrate.
- Existing runtime policy loading and schema loading utilities remain authoritative.
