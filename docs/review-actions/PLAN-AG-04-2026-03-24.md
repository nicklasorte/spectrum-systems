# Plan — AG-04 — 2026-03-24

## Prompt type
PLAN

## Roadmap item
AG-04 — Override Artifact Enforcement

## Objective
Implement a deterministic, fail-closed override-decision artifact and bounded runtime enforcement path so AG-03 review-required halts can only resume via explicit governed override semantics.

## Declared files

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-AG-04-2026-03-24.md | CREATE | Required PLAN artifact before multi-file BUILD work |
| PLANS.md | MODIFY | Register AG-04 plan in active plans table |
| contracts/schemas/hitl_override_decision.schema.json | CREATE | Canonical AG-04 override artifact contract |
| contracts/examples/hitl_override_decision.json | CREATE | Deterministic governed example payload for AG-04 contract |
| contracts/standards-manifest.json | MODIFY | Publish AG-04 contract and bump standards manifest version |
| spectrum_systems/modules/runtime/agent_golden_path.py | MODIFY | Enforce bounded override semantics at review-required boundary |
| scripts/run_agent_golden_path.py | MODIFY | Add minimal CLI override input + fail-closed exit semantics |
| tests/test_agent_golden_path.py | MODIFY | Add focused runtime assertions for override gating behavior |
| tests/test_hitl_override_enforcement.py | CREATE | Targeted AG-04 schema/runtime determinism and fail-closed tests |
| docs/runtime/hitl_override_enforcement.md | CREATE | Document AG-04 artifact and enforcement behavior |
| docs/runtime/agent_golden_path.md | MODIFY | Reference AG-04 resume semantics from AG-01/AG-03 runtime doc |

## Contracts touched
- contracts/schemas/hitl_override_decision.schema.json (new)
- contracts/standards-manifest.json (version bump + registry entry)

## Tests that must pass after execution

1. `pytest tests/test_agent_golden_path.py tests/test_hitl_override_enforcement.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py tests/test_hitl_review_queue.py`
3. `python scripts/run_agent_golden_path.py --force-review-required --output-dir /tmp/ag04-review-required`
4. `python scripts/run_agent_golden_path.py --force-review-required --override-decision-path contracts/examples/hitl_override_decision.json --output-dir /tmp/ag04-override-allow`
5. `.codex/skills/verify-changed-scope/run.sh docs/review-actions/PLAN-AG-04-2026-03-24.md`

## Scope exclusions

- Do not redesign AG-01/AG-03 architecture, queueing, or orchestration.
- Do not add UI/workflow engine concepts.
- Do not broaden control/eval authority or bypass fail-closed policies.
- Do not implement open-ended or heuristic override logic.

## Dependencies

- AG-03 HITL review-request handoff path must remain authoritative.
- Existing control/eval enforcement contracts and runtime semantics remain unchanged outside AG-04 boundary wiring.
