# Plan — D3L-03 — 2026-04-27

## Prompt type
BUILD

## Roadmap item
D3L-03

## Objective
Diagnose why the deployed 3LS dashboard does not surface the flow diagram and apply the smallest fail-closed fix so deployment targets the correct app and system-flow states are explicitly visible.

## Declared files

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-D3L-03-2026-04-27.md | CREATE | Record plan before multi-file BUILD execution. |
| PLANS.md | MODIFY | Register active D3L-03 plan per repository plan policy. |
| apps/dashboard-3ls/app/api/system-flow/route.ts | MODIFY | Enforce deterministic response envelope for ok/missing/invalid_schema states. |
| apps/dashboard-3ls/app/page.tsx | MODIFY | Ensure explicit 3LS flow panel labeling and visible degraded-state messaging. |
| apps/dashboard-3ls/__tests__/api/system-flow.test.ts | CREATE | Validate /api/system-flow ok/missing/invalid_schema behavior. |
| apps/dashboard-3ls/__tests__/components/DashboardPage.test.tsx | MODIFY | Verify explicit degraded-state flow panel rendering. |
| scripts/deploy-3ls-dashboard.sh | MODIFY | Correct deployment/build path so 3LS deployment executes dashboard-3ls codepath. |

## Contracts touched
None.

## Tests that must pass after execution
1. `cd apps/dashboard-3ls && npm install`
2. `cd apps/dashboard-3ls && npm run build`
3. `cd apps/dashboard-3ls && npm test -- --runTestsByPath __tests__/api/system-flow.test.ts __tests__/components/DashboardPage.test.tsx`

## Scope exclusions
- Do not redesign dashboard architecture beyond flow visibility and fail-closed state presentation.
- Do not change canonical system ownership in architecture documents.
- Do not introduce non-deterministic API fallback behaviors.

## Dependencies
- Existing flow artifact at `artifacts/tls/system_registry_dependency_graph.json` remains the canonical source for flow edges/nodes.
