# Plan — VAL-04 Control Decision Consistency — 2026-03-28

## Prompt type
PLAN

## Roadmap item
VAL-04 — Control Decision Consistency

## Objective
Implement a governed validation slice that repeatedly executes the real evaluation control decision seam on identical governed inputs, detects deterministic divergence, and emits a fail-closed `control_decision_consistency_result` artifact.

## Declared files
List every file that will be created, modified, or deleted.
No other files may be changed during execution of this plan.

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-VAL-04-2026-03-28.md | CREATE | Required PLAN artifact before multi-file VALIDATE implementation. |
| contracts/schemas/control_decision_consistency_result.schema.json | CREATE | Contract for governed VAL-04 validation artifact output. |
| contracts/examples/control_decision_consistency_result.json | CREATE | Deterministic golden-path artifact example for the new contract. |
| contracts/standards-manifest.json | MODIFY | Register `control_decision_consistency_result` contract version pin. |
| spectrum_systems/modules/governance/control_decision_consistency.py | CREATE | Governed VAL-04 validation module that exercises real evaluation-control seam. |
| spectrum_systems/modules/governance/__init__.py | MODIFY | Export VAL-04 validation API from governance package. |
| scripts/run_control_decision_consistency_validation.py | CREATE | CLI for governed inputs, artifact emission, and non-zero FAILED exit behavior. |
| tests/test_control_decision_consistency.py | CREATE | Deterministic tests for required consistency/divergence/fail-closed behavior. |

## Contracts touched
- Create `contracts/schemas/control_decision_consistency_result.schema.json`.
- Add `control_decision_consistency_result` entry in `contracts/standards-manifest.json`.

## Tests that must pass after execution
1. `pytest tests/test_control_decision_consistency.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
3. `python scripts/run_contract_enforcement.py`
4. `.codex/skills/verify-changed-scope/run.sh`

## Scope exclusions
- Do not modify control semantics or thresholds in runtime control modules.
- Do not introduce a new control loop or probabilistic behavior.
- Do not bypass schema validation at any boundary.

## Dependencies
- Runtime evaluation control seam (`build_evaluation_control_decision`) remains authoritative and unchanged.
- Existing replay/eval/error-budget/monitor/xrun contracts remain available for deterministic input adaptation.
