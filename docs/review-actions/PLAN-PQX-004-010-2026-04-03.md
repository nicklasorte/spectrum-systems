# Plan — PQX-004..PQX-010 Core Hardening Batch — 2026-04-03

## Prompt type
PLAN

## Roadmap item
PQX-004..PQX-010 governed sequential execution hardening batch

## Objective
Harden the existing PQX sequential batch path so admission is sealed pre-execution, termination is explicit, trace/state artifacts are replayable, and deterministic parity checks fail closed on divergence.

## Declared files
List every file that will be created, modified, or deleted.
No other files may be changed during execution of this plan.

| File | Change type | Reason |
| --- | --- | --- |
| docs/review-actions/PLAN-PQX-004-010-2026-04-03.md | CREATE | Required PLAN artifact before multi-file BUILD work. |
| spectrum_systems/modules/runtime/pqx_sequence_runner.py | MODIFY | Implement admission snapshot sealing, run hash/fingerprint, termination_reason, replayable snapshot, trace completeness, and determinism comparison logic. |
| contracts/schemas/prompt_queue_sequence_run.schema.json | MODIFY | Extend canonical persisted state contract with new deterministic hardening fields. |
| contracts/examples/prompt_queue_sequence_run.json | MODIFY | Keep golden example aligned with updated prompt_queue_sequence_run schema. |
| contracts/schemas/prompt_queue_replay_record.schema.json | MODIFY | Extend replay parity contract to include required determinism comparison invariants. |
| contracts/examples/prompt_queue_replay_record.json | MODIFY | Keep replay record example aligned with updated replay parity schema. |
| contracts/standards-manifest.json | MODIFY | Version bump and schema metadata update for modified prompt_queue_sequence_run contract. |
| tests/test_pqx_sequence_runner.py | MODIFY | Add/adjust tests for invariants: admission artifact, termination reason, replayable snapshot, trace completeness, and determinism parity checks. |

## Contracts touched
- `contracts/schemas/prompt_queue_sequence_run.schema.json` (additive schema update)
- `contracts/schemas/prompt_queue_replay_record.schema.json` (additive schema update)
- `contracts/standards-manifest.json` (version and contract metadata update)

## Tests that must pass after execution
1. `pytest tests/test_pqx_sequence_runner.py`
2. `pytest tests/test_contracts.py tests/test_contract_enforcement.py`
3. `python scripts/run_contract_enforcement.py`

## Scope exclusions
- Do not introduce new orchestrator layers or alternate execution paths.
- Do not refactor unrelated runtime modules outside `pqx_sequence_runner.py`.
- Do not modify non-PQX contracts unrelated to `prompt_queue_sequence_run`.

## Dependencies
- Existing sequential execution path (`execute_sequence_run`) remains the canonical path.
- PQX per-slice eval/control/enforcement path remains unchanged and reused.
