# Reviews Directory

Store immutable design/architecture review artifacts here using the canonical format in `../design-review-standard.md`. Filenames should include the date and short scope (e.g., `2026-03-14-architectural-review.md`). Each artifact must be linked from `../review-registry.md`.
