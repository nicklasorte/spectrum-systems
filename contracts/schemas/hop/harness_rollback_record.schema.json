{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spectrum-systems.org/contracts/hop/harness_rollback_record.schema.json",
  "title": "HOP Rollback Record",
  "description": "Advisory rollback / quarantine record emitted by the HOP rollback module. HOP records intent and structured evidence; the external control plane and SEL execute the action.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "artifact_id",
    "artifact_type",
    "schema_ref",
    "schema_version",
    "trace",
    "content_hash",
    "rollback_id",
    "subject_candidate_id",
    "previous_promoted_candidate_id",
    "action",
    "reason",
    "evidence",
    "advisory_only",
    "recorded_at"
  ],
  "properties": {
    "artifact_id": {"type": "string", "pattern": "^hop_rollback_[a-z0-9_-]+$"},
    "artifact_type": {"const": "hop_harness_rollback_record"},
    "schema_ref": {"const": "hop/harness_rollback_record.schema.json"},
    "schema_version": {"const": "1.0.0"},
    "content_hash": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "trace": {"$ref": "#/$defs/trace"},
    "rollback_id": {"type": "string", "pattern": "^[a-z0-9_-]+$"},
    "subject_candidate_id": {"type": "string", "pattern": "^[a-z0-9_-]+$"},
    "previous_promoted_candidate_id": {
      "type": ["string", "null"],
      "pattern": "^[a-z0-9_-]+$"
    },
    "action": {"type": "string", "enum": ["revert", "quarantine"]},
    "reason": {
      "type": "string",
      "enum": [
        "regression_detected",
        "blocking_failure_detected",
        "trace_incomplete",
        "control_plane_directive",
        "operator_request",
        "promotion_gate_block"
      ]
    },
    "evidence": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["kind", "detail"],
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "score_artifact_id",
              "failure_artifact_id",
              "promotion_decision_artifact_id",
              "trace_artifact_id",
              "snippet"
            ]
          },
          "detail": {"type": "string", "minLength": 1, "maxLength": 1024}
        }
      }
    },
    "advisory_only": {"const": true},
    "recorded_at": {"type": "string", "format": "date-time"}
  },
  "$defs": {
    "trace": {
      "type": "object",
      "additionalProperties": false,
      "required": ["primary", "related"],
      "properties": {
        "primary": {"type": "string", "minLength": 1},
        "related": {"type": "array", "uniqueItems": true, "items": {"type": "string", "minLength": 1}}
      }
    }
  }
}
