{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spectrum-systems.org/contracts/hop/harness_control_advisory.schema.json",
  "title": "Harness Control Advisory",
  "description": "The single advisory artifact the harness feedback surface emits for external control owners (CDE/REL/SEL). It is informational input only — the harness does not encode or claim release/advancement/control authority. Owners read the advisory and emit their own governed records.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "artifact_id",
    "artifact_type",
    "schema_ref",
    "schema_version",
    "trace",
    "content_hash",
    "advisory_id",
    "subject_candidate_id",
    "summary_kind",
    "release_readiness_signal_artifact_id",
    "trial_summary_artifact_id",
    "risk_failure_artifact_ids",
    "advisory_only",
    "delegates_to",
    "emitted_at"
  ],
  "properties": {
    "artifact_id": {"type": "string", "pattern": "^hop_advisory_[a-z0-9_-]+$"},
    "artifact_type": {"const": "hop_harness_control_advisory"},
    "schema_ref": {"const": "hop/harness_control_advisory.schema.json"},
    "schema_version": {"const": "1.0.0"},
    "content_hash": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "trace": {"$ref": "#/$defs/trace"},
    "advisory_id": {"type": "string", "pattern": "^[a-z0-9_-]+$"},
    "subject_candidate_id": {"type": "string", "pattern": "^[a-z0-9_-]+$"},
    "summary_kind": {
      "type": "string",
      "enum": ["readiness_evaluation", "rollback_signal_request", "trend_signal"]
    },
    "release_readiness_signal_artifact_id": {
      "type": ["string", "null"],
      "minLength": 1
    },
    "trial_summary_artifact_id": {
      "type": ["string", "null"],
      "minLength": 1
    },
    "risk_failure_artifact_ids": {
      "type": "array",
      "items": {"type": "string", "minLength": 1}
    },
    "advisory_only": {"const": true},
    "delegates_to": {
      "type": "array",
      "minItems": 1,
      "items": {"type": "string", "enum": ["REL", "CDE", "SEL"]}
    },
    "emitted_at": {"type": "string", "format": "date-time"}
  },
  "$defs": {
    "trace": {
      "type": "object",
      "additionalProperties": false,
      "required": ["primary", "related"],
      "properties": {
        "primary": {"type": "string", "minLength": 1},
        "related": {"type": "array", "uniqueItems": true, "items": {"type": "string", "minLength": 1}}
      }
    }
  }
}
